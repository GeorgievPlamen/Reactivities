import { List } from "semantic-ui-react";

export default observer(function ActivityListItemAttendee() {
  return (
    <List horizontal>
      <List.Item>
        <Image size />
      </List.Item>
    </List>
  );
});
